# Leave VITE_API_URL empty when running `npm run dev` — requests are proxied to
# the backend on :8000 by vite.config.ts. For a production build, set it to the
# deployed backend origin, e.g. https://api.example.com
VITE_API_URL=
